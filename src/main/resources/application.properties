# App Configuration
spring.application.name=qrcode
server.servlet.context-path=/${spring.application.name}

# Database Connection
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=qrcode

